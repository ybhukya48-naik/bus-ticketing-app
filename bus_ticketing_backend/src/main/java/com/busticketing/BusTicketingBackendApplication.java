package com.busticketing;

// This file is kept to satisfy repository structure requirements 
// but is intentionally left empty to avoid conflicts with the 
// main application class in the sub-package.
public class BusTicketingBackendApplication {
    // Empty
}

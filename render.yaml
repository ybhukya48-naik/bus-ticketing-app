services:
  # Backend Spring Boot Service
  - type: web
    name: bus-ticketing-backend
    env: docker
    plan: free
    rootDir: bus_ticketing_backend
    dockerfilePath: Dockerfile
    healthCheckPath: /api/health/ready
    envVars:
      - key: DATABASE_URL
        sync: false # Set this to your Neon DB connection string (postgres://...) in Render Dashboard
      - key: SPRING_DATASOURCE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: RAZORPAY_KEY_ID
        sync: false
      - key: RAZORPAY_KEY_SECRET
        sync: false
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: PORT
        value: 10000

  # Frontend Flutter Web Service
  - type: web
    name: bus-ticketing-app-web
    env: docker
    plan: free
    dockerfilePath: Dockerfile.web
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 80
